<section class="HIWBlock">
      <div id="HIWBlockTittle"></div>
      <div class="HIWBlockContent">
        
        <div class="screen">
          <img id="iphone-image" src="images\Content\run.webp" alt="Displayed Image">
        </div>
        <div id="HIWtext"></div>
      </div>
    </section>





















    
    const ClásicoSlot = document.getElementById("Clásico");
    if (ClásicoSlot) {

      const h3 = document.createElement("h3"); 
      const h1 = document.createElement("h1"); 
      const a =  document.createElement("a"); 
      const span =  document.createElement("span"); 
      
    
      h3.textContent = "Plan";
      
      h1.textContent = tittle;
      p.textContent = tittleText;
  
    
  
  
      
  
  
      freeText.appendChild(h1);
      freeText.appendChild(p);
  
  // Create a list container for the points
  const ul = document.createElement("ul");
  
  // Iterate over each item in the list array
  list.forEach((point) => {
    const li = document.createElement("li"); // Create a list item for each point
    li.textContent = point; // Set the text content of the list item
    ul.appendChild(li); // Append the list item to the ul element
  });
  
  // Append the list to the freeText container
  freeText.appendChild(ul);
    }







  
  function renderImage(imageUrl) {
    const imgContainer = document.getElementById("freeImg");
  
    // Create a new image element
    const img = document.createElement("img");
    img.src = imageUrl;  // Set the image source to the provided URL
    img.alt = "Wall";  // Set alt 
  
  
    // Clear any existing content in the container and add the new image
    imgContainer.innerHTML = "";
    imgContainer.appendChild(img);
  }
  
  // Example usage: Call renderImage with the URL of the image
  renderImage(img);
  





















  // Create and append cost information
      const costDiv = document.createElement("div");
      costDiv.className = "cost";

      const monthCostP = document.createElement("p");
      monthCostP.textContent = `Monthly: $${monthCost}`;

      const yearCostP = document.createElement("p");
      yearCostP.textContent = `Yearly: $${yearCost}`;

      costDiv.appendChild(monthCostP);
      costDiv.appendChild(yearCostP);
      div.appendChild(costDiv);

      // Create and append points list
      if (points && points.length > 0) {
        const ul = document.createElement("ul");

        points.forEach((point) => {
          const li = document.createElement("li");
          li.textContent = point;
          ul.appendChild(li);
        });

        div.appendChild(ul);
      }
















      /* Main container for the intro section */
#RevolFitAppIntro {
  width: 100%;
  height: 100vh; /* Full-screen height */
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  position: relative;
  color: #ffffff;
  font-family: 'Inter', sans-serif;
  overflow: hidden;
}

/* Overlay for text */
.intro-overlay {
  width: 100%;
  text-align: center;
  padding: 2rem;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
  position: absolute;
  bottom: 0;
}

/* Subtitle styling */
.intro-overlay h3 {
  font-size: 1rem;
  font-weight: 300;
  text-transform: uppercase;
  margin-bottom: 1rem;
  letter-spacing: 1.5px;
  opacity: 0.8;
}

/* Title styling */
.intro-overlay h1 {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  line-height: 1.5;
}

/* Start button styling */
.start-button {
  background-color: #013948;
  color: #ffffff;
  border: none;
  padding: 1rem 2rem;
  font-size: 1rem;
  font-weight: bold;
  text-transform: uppercase;
  border-radius: 5px;
  display: inline-flex;
  align-items: center;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.start-button:hover {
  background-color: #ACE2E4;
  color: #013948;
}

.start-button .arrow {
  margin-left: 0.5rem;
}

/* Logo container styling */
.logo-container {
  position: absolute;
  bottom: 1rem;
  text-align: center;
}

.intro-logo {
  width: 60px; /* Adjust size as needed */
  opacity: 0.8;
}





#status-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
 

  color: #000000;
  font-family: Arial, sans-serif;
  font-size: 14px;
}

#time {
  margin-left: 10px;
}

#wifi, #battery {
 background-color: aqua;
}



#RevolFitAppIntro{
  width: 100%;
  height: 100vh;
  object-fit: fill;
}